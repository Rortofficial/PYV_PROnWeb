@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer localizer
@model List<Client.Models.Gets.GetListSaleQuotation>
<title>@ViewData["Title"] - EWSystem</title>
<div class="row mt-5">
    <div class="col-12">
        <a asp-controller="BookingSheet" asp-action="Index" class="btn float-end" style="background-color:#01295C;color:white">
            Add NewData
        </a>
    </div>
</div>
<div class="table-responsive mt-3">
    <div class="row">
        <div class="col-12">
            <table id="TbListBookingSheet" class="table table-striped table-bordered dt-responsive nowrap" width="100%" cellspacing="0" style="background: #f0eff1; color: #696969;">
                <thead>
                    <tr>
                        <th>@localizer["CUSTOMER NAME"]</th>
                        <th>@localizer["QUOTATION DATE"]</th>
                        <th>@localizer["TEL"]</th>
                        <th>@localizer["CUSTOMER REF"]</th>
                        <th>@localizer["EMAIL"]</th>
                        <th>@localizer["ROUTE"]</th>
                        <th>@localizer["DOCTOTAL"]</th>
                        <th>@localizer["ACTION"]</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null)
                    {
                        @foreach(var a in Model){
                            <tr>
                                <td>@a.CustomerName</td>
                                <td>@a.QuotationDate</td>
                                <td>@a.Tel</td>
                                <td>@a.CustomerRefNo</td>
                                <td>@a.Email</td>
                                <td>@a.Route</td>
                                <td>@a.DocTotal</td>
                                <td>
                                    <button class="btn" style="float:right;background-color:#01295C;color:white" onclick="CopyTo('@Url.Action("Index","BookingSheet")?DocNum=@a.DocEntry')">Copy to</button>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
@section scripts{
    <script>
        $('#TbListBookingSheet').DataTable();
        function CopyTo(url){
            window.location.href=url;
        }
    </script>
}